보조 스트림
스트림의 기능을 향상시키거나 새로운 기능을 추가하기 위해 사용
독립적으로 입출력을 수행할 수 없다
버퍼를 채우는 동안 기다리는 시간 = 버퍼링
임시 저장 공간 = 버퍼

BufferedStream
가장 자주 쓰이는 보조 스트림
입, 출력 스트림에 버퍼(임시 메모리 영역)를 제공. 입출력 작업의 성능 향상

문자 한 개씩 읽는 것보다 버퍼를 이용해서 한 번에 읽는 것이 좀 더 효율적이다
기본 값 8192byte

바이트 버퍼 스트림
바이트 단위의 바이너리 데이터를 처리하는 버퍼 스트림

문자 버퍼 스트림

out.write();
버퍼가 꽉 팔 때 문자가 화면에 출력

new Scanner(System.in).nextLine(); - <Enter>키 기다림